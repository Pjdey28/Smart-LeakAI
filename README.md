SmartLeakAI:  AI system for Water Leakage Detection and Monitoring

To start :
Enter the command
python -m pip install -r requirements.txt

Then,
Enter this:
cd api
python api.py

Then,
Split terminal and enter this:
cd dashboard
streamlit run dashboard.py
